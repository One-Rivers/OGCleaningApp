name: og_scheduler
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10
  geolocator: ^13.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  bdd_widget_test: ^2.1.4
  build_runner: ^2.15.0
  flutter_driver:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    